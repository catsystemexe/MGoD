export interface DevParams {
  godMode: boolean;
  cellSize: number;
  genSpeed: number;
  spawnRate: number;
  timeScale: number;
  cover: boolean;
  crt: boolean;
}
export class DevUI {
  updateInput() {}
  render() {}
}
