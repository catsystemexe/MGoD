import { Vec2 } from "../../utils/math";

export class LootSystem {
    update(dt: number, playerPos: Vec2) { return null; }
    render(ctx: CanvasRenderingContext2D, cam: Vec2) {}
    spawn(pos: Vec2) {}
}
