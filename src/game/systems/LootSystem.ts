import { Vec2 } from "../../utils/math";

export class LootSystem {
    spawn(pos: Vec2) {
        // Placeholder for loot logic
    }
    
    update(dt: number, playerPos: Vec2) {
        return null;
    }
    
    render(ctx: CanvasRenderingContext2D, cam: Vec2) {
        // Placeholder render
    }
}
